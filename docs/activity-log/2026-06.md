# 2026-06 Activity Log

### 2026-06-04 11:18 - Implement spec-to-usecase artifact chain and review flow

- Category: implementation
- Request: Implement `TASK-072`, `TASK-073`, and `TASK-074` for the new upstream pipeline `spec -> use case -> diagram -> BRD`.
- Action: Added canonical artifact-chain docs, created backend schemas/service/route for deterministic `ProjectSpec + FeatureIntent -> UseCaseDraft[]`, wired a new frontend `Use case drafts` panel into the editor shell for ingestion/review/approve, and added route tests, UI tests, browser smoke, changelog, architecture docs, and a dedicated `UC-07`.
- Files: `apps/api/app/main.py`, `apps/api/app/routes/usecase_generate.py`, `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/tests/test_usecase_routes.py`, `src/App.tsx`, `src/styles.css`, `src/usecases/*`, `e2e/brd-flow.spec.ts`, `docs/scope/artifact-chain.md`, `docs/scope/architecture.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Next backlog should build `UseCaseDraft -> DiagramDraft` generation and round-trip traceability before formal BRD synthesis.
- Status: Done

### 2026-06-04 11:42 - Review TASK-072 to TASK-074 implementation

- Category: review
- Request: Re-review the just-implemented `artifact chain`, ingestion contract, and `spec -> use case list + UI review` slice.
- Action: Reviewed the new backend route/schema/builder, frontend use-case panel, docs contract, and tests. Confirmed the slice is a solid vertical baseline, then logged three follow-up risks: ingestion still lacks hard validation for empty/near-empty payloads, regenerate can silently overwrite reviewed use-case state, and the current deterministic builder still hard-codes a `2 + optional 1` UC segmentation that will not scale to real product specs.
- Files: `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/app/routes/usecase_generate.py`, `src/App.tsx`, `src/usecases/*`, `docs/reviews/2026-06-04-task-072-074-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-080`, `TASK-081`, and `TASK-082` before building `UseCaseDraft -> DiagramDraft`, so the upstream artifact quality is stable first.
- Status: Done

### 2026-06-04 12:08 - Harden spec-to-usecase ingestion and review workflow

- Category: implementation
- Request: Implement `TASK-080`, `TASK-081`, and `TASK-082` to tighten ingestion validation, protect reviewed use-case state on regenerate, and replace the rigid `2 + 1` builder heuristic.
- Action: Added backend normalization/validation for `ProjectSpec` and `FeatureIntent`, plus a global request-validation envelope for invalid payloads. Added frontend local pre-validation, disabled generate when required fields are missing, and introduced dirty-state + fingerprint-based outdated detection so regenerate now warns before replacing reviewed drafts. Refactored the deterministic use-case builder to support flexible segmentation (1 to 4 use cases) based on intake, coordination, and exception signals instead of always emitting two base UCs plus one exception UC. Expanded verification with builder tests, invalid-ingestion route tests, frontend helper tests, and browser E2E for regenerate cancel/accept flows.
- Files: `apps/api/app/schemas/usecase.py`, `apps/api/app/main.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/tests/test_usecase_builder.py`, `apps/api/tests/test_usecase_routes.py`, `src/usecases/prevalidate.ts`, `src/usecases/prevalidate.test.ts`, `src/usecases/UseCasePanel.tsx`, `src/App.tsx`, `src/styles.css`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: The next architectural milestone is still `UseCaseDraft -> DiagramDraft`, but now it can build on a more stable upstream contract and less brittle UC segmentation.
- Status: Done

### 2026-06-01 00:12 - Implement AI BRD completion tasks 023 to 026

- Category: implementation
- Request: Continue implementing the remaining AI BRD completion tasks for idempotency lifecycle, post-check traceability, sticky-note semantics, and sync-bar parallel interpretation.
- Action: Hardened `/generate` so non-success exit paths release idempotency entries and no longer poison retry keys. Refined BRD post-check to validate branch targets against a canonical traceability set instead of only `main_flow_steps`. Added sticky-note anchoring/global-note semantics using explicit anchor metadata or proximity within a lane. Upgraded `sync-bar` interpretation to emit more meaningful fork/join parallel blocks, including inferred downstream join candidates when available. Expanded backend regression tests for all four areas and reran mock-path verification.
- Files: `apps/api/app/idempotency.py`, `apps/api/app/routes/brd_generate.py`, `apps/api/app/services/postcheck.py`, `apps/api/app/services/validate.py`, `apps/api/app/services/interpret.py`, `apps/api/tests/test_routes.py`, `apps/api/tests/test_pipeline.py`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: `TASK-021` still needs live OpenRouter smoke with a real key, and `TASK-015` still needs full browser E2E coverage for the local mock app flow.
- Status: Done

### 2026-06-01 09:08 - Review AI BRD live readiness

- Category: review
- Request: Re-review the implemented AI BRD feature, advise what should happen next, and decide whether it is time to add a real API key to `.env` for testing.
- Action: Re-ran the mock-path verification suite, reviewed the live OpenRouter adapter, frontend generate flow, use-case contract, and current backlog. Confirmed the feature is strong on the mock path, but live provider usage is still unverified end-to-end, browser E2E coverage is still missing, and `UC-06` currently over-promises local frontend pre-validation versus runtime behavior.
- Files: `apps/api/app/providers/openrouter_provider.py`, `apps/api/app/routes/brd_generate.py`, `apps/api/tests/test_routes.py`, `src/App.tsx`, `src/brd/BrdPanel.test.tsx`, `src/brd/normalize.test.ts`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `docs/review-task-list.md`, `docs/reviews/2026-06-01-ai-brd-live-readiness-review.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-016` (manual live smoke with real key), then `TASK-017` (guarded live suite), `TASK-015` (browser E2E), and `TASK-027` (align frontend pre-validation with `UC-06`).
- Status: Done

### 2026-06-01 09:13 - Explain why TASK-027 exists

- Category: decision
- Request: Clarify why `TASK-027` is needed in the AI BRD backlog.
- Action: Explained that `TASK-027` exists to resolve a contract mismatch between `UC-06` and the implemented runtime: the doc promises browser-side pre-validation before backend calls, while the app currently validates through the backend first. Framed it as a doc/runtime alignment task rather than a defect in the BRD generation semantics.
- Files: `docs/review-task-list.md`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `src/App.tsx`, `docs/activity-log/2026-06.md`
- Follow-up: Decide whether to implement client-side pre-validation or update `UC-06` to explicitly document backend-first validation.
- Status: Decision

### 2026-06-01 09:58 - Implement AI BRD live harness, E2E, and local pre-validation

- Category: implementation
- Request: Continue implementing the remaining AI BRD tasks for live smoke with a real provider path, guarded live test commands, browser E2E for the BRD flow, and local frontend pre-validation matching `UC-06`.
- Action: Added automatic loading of `apps/api/.env` for local backend runtime, introduced `npm run test:api-live` with guarded OpenRouter smoke tests (happy path + replay), installed Playwright and added browser E2E coverage for `Generate BRD -> edit draft -> outdated -> export` plus a client-side pre-validation test that confirms invalid diagrams are blocked before any backend request. Implemented frontend local pre-validation for missing `start/end`, missing lane ownership, and invalid edge endpoints, then updated `UC-06`, changelog, and backlog state to match runtime.
- Files: `apps/api/app/config.py`, `apps/api/.env.example`, `apps/api/tests/test_live_smoke.py`, `src/App.tsx`, `src/brd/prevalidate.ts`, `src/brd/prevalidate.test.ts`, `e2e/brd-flow.spec.ts`, `playwright.config.ts`, `package.json`, `package-lock.json`, `vite.config.ts`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: `TASK-016` and `TASK-017` are now ready for a real OpenRouter smoke pass, but still need an actual `BRD_OPENROUTER_API_KEY` in `apps/api/.env` with `BRD_PROVIDER=openrouter` to move from harness-ready to verified live success.
- Status: Partial

### 2026-06-01 10:01 - Decide how to test live flow with a smaller model

- Category: decision
- Request: Clarify whether the team must change code to test the live AI BRD flow with a smaller model such as `gpt-4o-mini` before using the primary production model.
- Action: Confirmed that the current backend/provider path already reads the primary model from env, so model switching for live smoke is an environment-level decision rather than a code change. Noted the real caveat: the selected OpenRouter route still needs to behave correctly with the repository's strict structured-output contract.
- Files: `apps/api/app/config.py`, `apps/api/app/routes/brd_generate.py`, `apps/api/app/providers/openrouter_provider.py`, `apps/api/.env.example`, `docs/activity-log/2026-06.md`
- Follow-up: To test with a smaller model, set `BRD_PROVIDER=openrouter`, add `BRD_OPENROUTER_API_KEY`, change `BRD_MODEL_PRIMARY`, then run `npm run test:api-live`.
- Status: Decision

### 2026-06-01 10:39 - Run full live AI BRD test flow with real OpenRouter key

- Category: implementation
- Request: Test the full AI BRD flow after adding a real API key to `apps/api/.env`.
- Action: Verified the env is set for `BRD_PROVIDER=openrouter` with a real key present and `openai/gpt-4o-mini` as the active model. Reran the full mock suite and build successfully, then executed `npm run test:api-live`. Live smoke initially failed because strict structured output from OpenRouter/OpenAI rejected the generated JSON Schema; fixed this by normalizing the response schema to include `additionalProperties: false` and a complete `required` list for each object node. After the fix, live API smoke passed for both happy path and replay with the same `Idempotency-Key`. Also ran a manual UI live smoke on `http://127.0.0.1:5173`: `Generate BRD` succeeded, the BRD panel showed model `openai/gpt-4o-mini`, the draft was editable, and the `Outdated` badge appeared correctly after changing the diagram.
- Files: `apps/api/app/providers/openrouter_provider.py`, `apps/api/app/schemas/common.py`, `apps/api/app/schemas/request.py`, `apps/api/app/schemas/spec.py`, `apps/api/tests/test_schema_contract.py`, `apps/api/tests/test_live_smoke.py`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Live path is now verified with the smaller model. The next sensible step is to rerun `npm run test:api-live` with the intended production model before treating quality/cost numbers as final.
- Status: Done

### 2026-06-01 10:50 - Update README with full backend + frontend run guide

- Category: decision
- Request: Update the repository README so a teammate can run the complete flow locally, including backend, frontend, mock mode, live mode, and the main test commands.
- Action: Expanded the root `README.md` with full local setup instructions for frontend and FastAPI backend, `.env` configuration for both mock and OpenRouter live modes, manual BRD smoke steps, and the current mock/live test commands. This was a docs-only change; no runtime behavior changed in this step.
- Files: `README.md`, `docs/activity-log/2026-06.md`
- Follow-up: Keep README model examples aligned with the model currently recommended for live smoke and production verification.
- Status: Done

### 2026-06-01 10:54 - Explain BRD panel blocking error shown in UI

- Category: decision
- Request: Clarify the error shown in the BRD panel screenshot during BRD generation.
- Action: Explained that the panel is not reporting a provider/model failure; it is reporting frontend/backend validation blocking because the current diagram no longer contains the required `start` and `end` nodes. Also clarified that the yellow `Outdated` banner is separate and only means the diagram changed after a previous successful generate.
- Files: `src/App.tsx`, `src/brd/prevalidate.ts`, `apps/api/app/services/validate.py`, `docs/activity-log/2026-06.md`
- Follow-up: Re-add at least one `start` node and one `end` node, then generate again.
- Status: Decision

### 2026-06-01 19:38 - Review AI BRD output quality on real bomb-threat draft

- Category: review
- Request: Review a concrete AI BRD draft generated from a real bomb-threat diagram, explain why the output is hard to understand, and propose a fix direction.
- Action: Reviewed the BRD pipeline end-to-end and confirmed the main problem is not the model alone: the semantic pipeline currently flattens branch-heavy graphs into a linear main workflow, treats every cross-lane edge as a handoff, and leaks raw internal IDs into reader-facing markdown. Saved a review snapshot, opened a confirmed known issue for reader-facing BRD quality, and added follow-up tasks for branch-aware flow interpretation, handoff semantics, reader-facing rendering, and golden narrative-quality tests.
- Files: `apps/api/app/services/interpret.py`, `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/services/prompt_builder.py`, `apps/api/app/services/postcheck.py`, `docs/reviews/2026-06-01-ai-brd-output-quality-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-028` to `TASK-032` before trying to improve BRD readability by model tuning alone.
- Status: Done

### 2026-06-01 10:47 - Explain how to run backend and frontend locally for end-to-end testing

- Category: decision
- Request: Provide the exact local run steps for backend and frontend so the full BRD flow can be tested manually.
- Action: Documented the minimal backend and frontend commands, expected local URLs, and the simplest manual smoke sequence for `Generate BRD` using the existing live env setup.
- Files: `apps/api/.env`, `package.json`, `docs/activity-log/2026-06.md`
- Follow-up: After switching models in `apps/api/.env`, rerun the same local flow and optionally `npm run test:api-live`.
- Status: Decision

### 2026-06-01 19:58 - Implement reader-facing BRD quality tasks TASK-028 to TASK-032

- Category: implementation
- Request: Implement the BRD quality backlog so generated output reads like a usable BA document rather than a debug dump: separate the main spine from alternate paths, redefine handoffs, move raw ids into a debug appendix, improve parallel summaries, and add golden reader-facing tests.
- Action: Refactored `interpret.py` so main flow keeps only the preferred spine while decision branches now capture path summaries and rejoin points. Reworked handoff semantics to only emit meaningful cross-actor transitions, enriched sync-bar analysis into business-level parallel summaries, and rebuilt the markdown renderer to keep the first 10 BRD sections reader-facing while moving raw ids to `Appendix A`. Added spec harmonization so live provider output is normalized back to deterministic reader-facing fields, expanded pipeline/route tests with golden quality assertions, and isolated Playwright mock backend traffic onto port `18000` so E2E no longer reuses a live server on `8000`.
- Files: `apps/api/app/services/interpret.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/services/render.py`, `apps/api/app/routes/brd_generate.py`, `apps/api/app/services/prompt_builder.py`, `apps/api/app/schemas/spec.py`, `apps/api/tests/test_pipeline.py`, `apps/api/tests/test_routes.py`, `src/brd/types.ts`, `playwright.config.ts`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-run `npm run test:api-live` with the production model when you want to measure final narrative quality/cost on the upgraded reader-facing pipeline.
- Status: Done

### 2026-06-01 20:05 - Review current AI BRD draft for the fire-incident sample

- Category: review
- Request: Review the current AI BRD draft generated from the fire-incident sample and judge whether the output is now genuinely useful.
- Action: Reviewed the generated draft against the current semantic pipeline and renderer. Confirmed one semantic bug and two quality gaps still remain: the draft can invent `Parallel activities` for a non-branching `sync-bar`, can misclassify a contextual fire-source note as a step annotation, and still narrates main-path decision outcomes in a mechanically serialized style. Saved a review snapshot, opened two specific known issues, and added `TASK-033` to `TASK-035` to finish the next quality pass.
- Files: `apps/api/app/services/interpret.py`, `apps/api/app/services/render.py`, `src/lf-config.ts`, `docs/reviews/2026-06-01-ai-brd-fire-incident-draft-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-033` and `TASK-034` before spending time on pure wording polish.
- Status: Done

### 2026-06-01 20:13 - Implement AI BRD polish tasks TASK-033 to TASK-035

- Category: implementation
- Request: Implement the next BRD quality fixes: remove false-positive parallel output for non-branching sync-bars, separate context notes from step annotations, and make decision narrative less mechanical.
- Action: Updated the semantic interpreter so linear `sync-bar` nodes no longer emit reader-facing `parallel_blocks`, added note classification (`step_annotation`, `context_note`, `global_note`) with `context_notes` flowing into the spec and renderer, and added a `continues_main_flow` flag so decision outcomes on the preferred path render as `Tiếp tục: ...` instead of always saying they rejoin the main flow. Expanded pipeline regressions for all three cases, then reran mock suite, live smoke, and build successfully.
- Files: `apps/api/app/services/interpret.py`, `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/schemas/spec.py`, `apps/api/tests/test_pipeline.py`, `src/brd/types.ts`, `src/brd/BrdPanel.test.tsx`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-generate the fire-incident draft and re-review the remaining narrative quality before investing in broader BA-facing export polish.
- Status: Done

### 2026-06-01 20:18 - Review AI BRD draft v2 for the fire-incident sample

- Category: review
- Request: Review the newer AI BRD draft for the fire-incident diagram and judge whether the output is now genuinely usable.
- Action: Compared the supplied draft against the current interpreter/renderer and confirmed one real renderer bug plus two remaining narrative-quality gaps. The good news is the previous semantic regressions are actually fixed: there is no longer a false parallel block and the fire-source note is no longer attached to a single step. The remaining concrete defect is that section 10 can still contradict itself when `context_notes` exist, because the empty-state guard ignores that field. Saved a new review snapshot, opened a confirmed known issue for the contradictory empty-state, and added follow-up tasks for the renderer empty-state, alternate-branch wording, and clearer trigger/start-event presentation.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/interpret.py`, `docs/reviews/2026-06-01-ai-brd-fire-incident-draft-v2-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-036` first, then decide whether `TASK-037` or `TASK-038` gives the next best BA-facing improvement.
- Status: Done

### 2026-06-01 20:24 - Implement AI BRD polish tasks TASK-036 to TASK-038

- Category: implementation
- Request: Fix the contradictory section-10 empty state, make alternate decision branches read less like graph traces, and clarify the opening trigger of the process in the reader-facing BRD draft.
- Action: Updated the BRD renderer so section 10 now treats `context_notes` as real content before deciding to show the empty-state line. Reworked alternate branch wording to use narrative sequencing (`Thực hiện lần lượt ...`) instead of raw arrow traces, while preserving `Tiếp tục: ...` for main-path continuations. Moved `Process overview` to a deterministic summary built from trigger context and the first operational actor, then harmonized live-provider output to use that summary too. Added regression coverage for all three changes and reran backend, mock, live, and build verification successfully.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/tests/test_pipeline.py`, `apps/api/tests/test_routes.py`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-generate the fire-incident draft and re-review whether the new `Process overview` and alternate-branch wording are BA-facing enough, or whether we should do one more polish pass on export prose.
- Status: Done

### 2026-06-01 20:34 - Review AI BRD draft v3 for the fire-incident sample

- Category: review
- Request: Review the latest AI BRD draft for the fire-incident diagram after the trigger/context and branch-wording fixes.
- Action: Confirmed the draft has crossed an important quality threshold: the prior semantic issues are gone, the trigger/process overview is now explicit, and section 10 no longer contradicts itself. The remaining issues are now mostly export-polish concerns rather than correctness bugs: reader-facing sections still leak canvas line breaks, `Context` notes are semantically right but not yet formatted like a polished document section, and some empty-state text still uses technical wording such as `parallel block`. Saved a new review snapshot and added three follow-up polish tasks for text normalization, context formatting, and BA-facing empty-state wording.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/services/interpret.py`, `docs/reviews/2026-06-01-ai-brd-fire-incident-draft-v3-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-039` and `TASK-040` if the next goal is export quality rather than semantic correctness.
- Status: Done

### 2026-06-01 20:37 - Implement AI BRD export-polish tasks TASK-039 to TASK-041

- Category: implementation
- Request: Continue polishing AI BRD output by normalizing canvas whitespace, rendering `Context` notes in a clearer document structure, and replacing technical empty-state wording with BA-facing phrasing.
- Action: Added a dedicated reader-facing text helper module so multiline canvas text is collapsed into readable prose before hitting the BRD draft. Applied that normalization across main workflow descriptions, decision labels, branch summaries, handoff reasons, and annotation/assumption rendering. Reworked `Context` note rendering so multiline notes now appear as a lead-in bullet with structured sub-bullets, and updated empty-state copy for decision, parallel, handoff, and assumptions/open-questions sections to sound more like a document than a tool dump. Locked the behavior with new pipeline regressions and reran backend, mock, and build verification successfully.
- Files: `apps/api/app/services/reader_text.py`, `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/services/interpret.py`, `apps/api/tests/test_pipeline.py`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-generate the fire-incident draft and confirm whether the remaining polish work should focus on business-objective prose and export formatting, not semantic interpretation.
- Status: Done

### 2026-06-02 09:33 - Review AI BRD draft v4 for the fire-incident sample

- Category: review
- Request: Review the latest fire-incident BRD draft after whitespace normalization, context formatting, and BA-facing empty-state polish.
- Action: Confirmed the draft has largely crossed from semantic repair into prose polish. The current output no longer leaks canvas line breaks, the `Context` block is structured cleanly, and the main reader-facing sections are now broadly understandable to a BA reviewer. The remaining gaps are mostly about deterministic prose quality: `Business objective` is still a static placeholder, `Process overview` still opens with generic wording instead of the specific fire-incident domain, and section 10’s title still feels slightly mismatched when it contains only context. Saved a fresh review snapshot and added follow-up tasks for objective generation, domain-specific overview wording, and optional section-10 label polish.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `docs/reviews/2026-06-02-ai-brd-fire-incident-draft-v4-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-042` and `TASK-043` if the next goal is a stronger BRD draft for human review rather than more semantic correctness work.
- Status: Done

### 2026-06-02 09:41 - Explain the intent of TASK-042 to TASK-044

- Category: decision
- Request: Clarify the practical meaning of `TASK-042`, `TASK-043`, and `TASK-044` in the AI BRD backlog so the user can understand why these tasks matter and what quality gap each one addresses.
- Action: Explained each task in plain language: `TASK-042` targets the still-generic `Business objective`, `TASK-043` targets the still-template-like opening of `Process overview`, and `TASK-044` targets the slight mismatch between section-10 title and its current `Context`-only content. Framed them as prose-quality/export tasks rather than semantic-correctness fixes.
- Files: `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: If the next goal is “BRD đủ đẹp để gửi người khác đọc”, prioritize `TASK-042` then `TASK-043`; keep `TASK-044` as optional polish unless section naming starts to bother reviewers.
- Status: Decision

### 2026-06-02 09:40 - Implement AI BRD prose tasks TASK-042 to TASK-044

- Category: implementation
- Request: Replace the static `Business objective` placeholder with real content, make `Process overview` sound more like the actual fire-incident domain instead of a generic template, and resolve the mismatch between section-10 title and its `Context`-heavy content.
- Action: Implemented deterministic prose generation in the BRD renderer for `Business objective`, using domain heuristics for fire-incident and bomb-threat style diagrams plus a safe generic fallback. Upgraded `Process overview` to infer a more domain-specific opening sentence from the diagram name, context notes, and main flow. Resolved the section-10 naming mismatch by keeping the Phase 1 10-section contract but renaming the final section to `Context / assumptions / open questions`, then synchronized the product doc and UC-06 to the new label. Added golden assertions for the fire-incident objective, the domain-specific overview, and the updated section title, and reran backend tests and frontend build successfully.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/tests/test_pipeline.py`, `docs/product/ai-brd-description-feature.md`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-generate the fire-incident BRD and decide whether the next prose-quality pass should focus on export formatting and business-language smoothing rather than structure.
- Status: Done

### 2026-06-02 10:09 - Fix OpenRouter `IncompleteRead` crash on live BRD generate

- Category: implementation
- Request: Diagnose and fix a live `500 Internal Server Error` during `POST /api/brd/generate`, where the backend trace showed `http.client.IncompleteRead` while reading the OpenRouter chunked response.
- Action: Confirmed the failure was not in BRD semantic generation but in the provider transport layer: `openrouter_provider.py` was calling `response.read()` directly and letting `IncompleteRead` escape as an unhandled exception. Hardened the provider to catch `IncompleteRead`, `RemoteDisconnected`, and related transport exceptions, then map them to `OpenRouterProviderError(retryable=True)` so `/generate` can preserve its retry/502 contract instead of crashing with `500`. Added a dedicated provider regression test, reran the full backend suite plus frontend build, and verified the live OpenRouter smoke path still passes after the transport hardening.
- Files: `apps/api/app/providers/openrouter_provider.py`, `apps/api/tests/test_openrouter_provider.py`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: If the same symptom reappears in production-like conditions, the next place to inspect is upstream connection stability or retry/backoff policy rather than BRD semantic rendering.
- Status: Done

### 2026-06-02 15:18 - Explain `Main workflow trace` in AI BRD draft

- Category: decision
- Request: Clarify the meaning of the `Main workflow trace` appendix block in a generated AI BRD draft.
- Action: Confirmed from the renderer that the appendix is a debug/traceability mapping, not reader-facing BRD prose. Explained that each `Sxx -> node_id` line links the human-readable main workflow step number back to the underlying diagram node id used internally by the system.
- Files: `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `docs/activity-log/2026-06.md`
- Follow-up: If appendix debug content feels noisy for end users, consider adding a UI toggle or export option to hide `Appendix A` in reader-facing BRD exports.
- Status: Decision

### 2026-06-02 15:31 - Review whether AI BRD `Main workflow` is too terse for BRD use

- Category: review
- Request: Assess whether section 5 `Main workflow` in the AI BRD draft is still too brief and not explicit enough for a BRD-style explanation of business steps.
- Action: Reviewed the current render path and confirmed the concern is valid. The generator currently builds each main-flow step from the diagram node text plus actor name only, then renders it as a flat numbered list. This is good for traceability and deterministic output, but still too thin for a BRD that needs step intent, business action, handoff meaning, and expected outcome to be obvious to a business reader.
- Files: `apps/api/app/services/spec_builder.py`, `apps/api/app/services/render.py`, `docs/activity-log/2026-06.md`
- Follow-up: The next quality upgrade should enrich `main_flow_steps` with BRD-facing detail such as step purpose, trigger/input, action, and expected result, or render a two-layer format where each numbered step has a short heading plus 1-2 explanatory sub-lines.
- Status: Done

### 2026-06-02 15:33 - Review AI BRD draft for BRD realism and define next improvement tasks

- Category: review
- Request: Review the latest AI-generated BRD draft again and define concrete tasks to move the output closer to a real BRD draft.
- Action: Confirmed the draft is now semantically stable but still lacks BRD-level richness in section 3, 4, and especially 5. Captured a new review snapshot focused on BRD realism and added actionable backlog items for richer `MainFlowStep` schema, expanded section 5 rendering, actor responsibilities, process-boundary scope content, optional debug appendix export, and prose-quality golden tests.
- Files: `docs/reviews/2026-06-02-ai-brd-brd-realism-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-045` and `TASK-046` first; they address the biggest remaining gap between the current draft and a BRD that feels real to business readers.
- Status: Done

### 2026-06-02 16:46 - Implement AI BRD realism tasks TASK-045 to TASK-050

- Category: implementation
- Request: Implement the next BRD-quality tasks: enrich `MainFlowStep`, render section 5 in a BRD-like expanded format, add actor responsibilities, make `Scope` describe process boundaries, separate reader-facing export from debug appendix, and add stronger golden tests.
- Action: Expanded the BRD spec schema and deterministic builder so each main-flow step now carries BRD-facing detail (`step_title`, `step_purpose`, `business_action`, `expected_result`, `input_or_trigger`). Reworked section 3, 4, and 5 rendering to read more like a real BRD, inferred actor responsibilities, and switched export modes so `template=default` is reader-facing while `template=full` keeps `Appendix A. Traceability (debug)`. Updated route and pipeline tests, reran full backend tests, frontend mock suite, frontend build, and live OpenRouter smoke successfully. Synced feature/use-case docs and marked TASK-045 to TASK-050 done.
- Files: `apps/api/app/schemas/spec.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/services/render.py`, `apps/api/tests/test_pipeline.py`, `apps/api/tests/test_routes.py`, `src/brd/types.ts`, `docs/product/ai-brd-description-feature.md`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-generate representative BRD drafts (cháy, đe dọa bom, generic flow) and review whether the next polish pass should target section-6 narrative or step-level prose specificity.
- Status: Done

### 2026-06-02 22:45 - Review frontend caching approach for AI BRD draft

- Category: review
- Request: Evaluate the idea of caching the generated AI BRD draft on the frontend before a database exists, because closing the BRD panel currently feels like losing the draft and forcing a new generation.
- Action: Reviewed the current BRD state flow in `App.tsx` and confirmed the idea is sound. The current implementation keeps BRD data only in React memory, has no persistent storage, and only reopens the panel via the generate flow, so the UX can easily feel like the draft was lost even though close currently only hides the panel. Captured a review snapshot and added a staged backlog for cache contract, localStorage persistence, reopen UX, outdated invalidation, manual discard, and automated coverage.
- Files: `src/App.tsx`, `src/brd/BrdPanel.tsx`, `docs/reviews/2026-06-02-ai-brd-frontend-cache-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-051` to `TASK-054` first; they establish the minimal usable frontend cache before optional cleanup UX and broader test coverage.
- Status: Done

### 2026-06-02 22:56 - Implement frontend cache for AI BRD draft continuity

- Category: implementation
- Request: Implement `TASK-051` to `TASK-056` so generated BRD drafts survive panel close and page reload via frontend cache, can be reopened explicitly, become `Outdated` when the diagram changes, and can be discarded manually.
- Action: Added a versioned frontend cache contract (`BrdWorkspaceCacheEntry`) plus `localStorage` helpers, wired `App.tsx` to hydrate/persist last BRD snapshot, added toolbar actions `Open last BRD draft` and `Discard cached BRD`, and kept reset/import/clear as context-switch events that preserve the old snapshot in `Outdated` state until user regenerates or discards it. Expanded Playwright to cover close/reopen/reload/outdated/discard and added Vitest coverage for cache save/load/version guard. Synced the feature doc, UC-06, changelog, and backlog statuses to the new cache policy.
- Files: `src/brd/cache.ts`, `src/brd/cache.test.ts`, `src/brd/types.ts`, `src/App.tsx`, `src/brd/BrdPanel.tsx`, `e2e/brd-flow.spec.ts`, `docs/product/ai-brd-description-feature.md`, `docs/use-cases/UC-06-sinh-brd-tu-diagram.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: The next natural polish pass is optional UX cleanup such as surfacing cache age (`updatedAt`) or adding a dedicated panel-level discard action if users ask for more visible cache management.
- Status: Done

### 2026-06-02 23:08 - Review and define tasks for draw.io XML import/export

- Category: review
- Request: Review the plan to temporarily hide `Mở JSON`, `Lưu JSON`, and `Export SVG`, then replace them with `Import XML` and `Export XML` using draw.io XML compatible with `examples/bomb.drawio.xml`.
- Action: Reviewed the current toolbar/import-export runtime and confirmed this is a reasonable direction, but it needs to be treated as a proper file-interchange feature rather than a UI label swap. Captured a review snapshot showing that the current flow is tightly coupled to repo-native JSON while the target draw.io file is a nested `mxGraphModel` tree with swimlane parents, style strings, and HTML-rich text. Added a new task sequence for contract definition, adapter extraction, XML parser, XML serializer, toolbar swap, geometry hardening, docs sync, and regression coverage.
- Files: `src/App.tsx`, `README.md`, `examples/bomb.drawio.xml`, `docs/reviews/2026-06-02-drawio-xml-import-export-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Start with `TASK-057` to `TASK-060`; they define the interchange contract and the parser/serializer core before any toolbar swap.
- Status: Done

### 2026-06-02 23:29 - Implement draw.io XML import/export workflow

- Category: implementation
- Request: Implement `TASK-057` to `TASK-064`: define the draw.io XML interchange contract, extract XML adapters from `App.tsx`, support draw.io XML import/export, switch the toolbar to XML-first, harden geometry mapping, update docs/workflow, and add regression coverage.
- Action: Added a lightweight XML adapter layer (`drawio-import`, `drawio-export`, shared text/style helpers, and graph types), then wired the editor toolbar to `Import XML…` and `Export XML` while hiding JSON/SVG actions from the main UI. Implemented import normalization from draw.io `mxfile/diagram/mxGraphModel/root` into lane-aware LogicFlow graph data, export serialization back to draw.io-compatible XML, and geometry/text heuristics for lanes, notes, sync bars, and edge labels. Synced README, UC-05, architecture docs, changelog, and task statuses to the new XML-first workflow. Verified with `npm run build`, `npm run test:ui-mock`, `npm run test:api-mock`, and `npm run test:e2e-mock`.
- Files: `src/App.tsx`, `src/io/drawio-import.ts`, `src/io/drawio-export.ts`, `src/io/drawio-shared.ts`, `src/io/drawio-types.ts`, `src/io/drawio-xml.test.ts`, `src/lane-config.ts`, `src/nodes.ts`, `src/lf-config.ts`, `e2e/brd-flow.spec.ts`, `README.md`, `docs/use-cases/UC-05-import-export.md`, `docs/scope/architecture.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: If users need broader draw.io compatibility beyond the Phase 1 supported subset, the next lane should be richer style preservation and a larger fixture set from real-world diagrams.
- Status: Done

### 2026-06-04 10:06 - Review AI BRD format gap against `examples/BRD.docx.md`

- Category: review
- Request: Read [examples/BRD.docx.md](/Users/quanliver/Projects/AI_Sys/swimlane-activity-diagram/examples/BRD.docx.md:1) and assess what must change so AI-generated BRD drafts can look much closer to that formal BRD style.
- Action: Compared the sample BRD template against the current AI BRD schema, deterministic builder, and renderer. Confirmed the gap is structural, not just prose-level: the current system emits a 10-section process-summary document, while the sample is a formal use-case document with business scope tables, actor-role tables, consolidated UC catalog, business state catalogs, and per-UC subsections for objectives, preconditions, main flow, state flow, and exceptions. Captured a review snapshot and added a new task batch for `formal_brd` contract definition, schema redesign, UC segmentation, business-state inference, table-first rendering, system-actor inference, and golden acceptance coverage.
- Files: `examples/BRD.docx.md`, `apps/api/app/services/render.py`, `apps/api/app/services/spec_builder.py`, `apps/api/app/schemas/spec.py`, `docs/reviews/2026-06-04-ai-brd-formal-template-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Start with `TASK-065` and `TASK-066` first. Without a separate `formal_brd` contract and schema, the rest of the work will just keep stretching the wrong output model.
- Status: Done

### 2026-06-04 10:22 - Define development order for `spec -> use case -> diagram -> formal BRD`

- Category: review
- Request: Recommend the best development order to reach the target workflow where the user inputs a project spec and desired function, the system generates use cases, then activity diagrams per use case, and finally a formal BRD in the target sample format.
- Action: Reviewed the current project scope, AI BRD feature, and roadmap, then concluded the next expansion should not start from “prettier BRD output” alone. Captured a roadmap review that defines the canonical artifact chain (`ProjectSpec -> FeatureIntent -> UseCaseDraft -> DiagramDraft -> FormalBRDDraft`), recommends building `spec -> use case` before `use case -> diagram`, and positions formal BRD synthesis after use-case and diagram contracts are stable. Added a new task batch for artifact-chain definition, spec ingestion, use-case generation/review, diagram generation, round-trip consistency, formal BRD synthesis, persistence/versioning, and pipeline eval. Also linked the direction from the roadmap overview.
- Files: `docs/roadmap/README.md`, `docs/reviews/2026-06-04-spec-to-usecase-diagram-brd-roadmap-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: The highest-leverage starting point is `TASK-072` to `TASK-074`; they create the missing use-case layer that the whole target workflow depends on.
- Status: Done

### 2026-06-04 13:10 - Review implementation quality of TASK-080 to TASK-082

- Category: review
- Request: Re-review the newly implemented `TASK-080` to `TASK-082` covering ingestion validation/normalization, review-state protection on regenerate, and flexible use-case segmentation.
- Action: Inspected the backend use-case schema, builder heuristics, frontend pre-validation flow, and regenerate/dirty-state logic. Confirmed the batch is a solid baseline but identified three follow-ups: the review-protection dirty flag is still sticky and can cause false-positive overwrite prompts, the frontend/backend validation boundary is not yet explicitly hardened as a shared contract, and segmentation quality still needs domain-grade golden fixtures before it should be treated as mature upstream input for diagram generation. Captured a new review snapshot and added follow-up tasks `TASK-083` to `TASK-085`.
- Files: `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/app/main.py`, `src/App.tsx`, `src/usecases/prevalidate.ts`, `src/usecases/UseCasePanel.tsx`, `apps/api/tests/test_usecase_builder.py`, `apps/api/tests/test_usecase_routes.py`, `docs/reviews/2026-06-04-task-080-082-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-083` first; it is the only user-facing workflow defect in this review and is the easiest fix to land before moving deeper into `UseCaseDraft -> DiagramDraft`.
- Status: Done

### 2026-06-04 20:15 - Implement TASK-083 to TASK-085 for use-case generation hardening

- Category: implementation
- Request: Implement `TASK-083` to `TASK-085`: replace sticky `useCaseDirty` with snapshot/fingerprint-derived state, formalize the frontend/backend validation contract, and add domain-grade golden fixtures for use-case segmentation.
- Action: Reworked the use-case review flow so overwrite protection is now derived from canonical spec fingerprint plus `UseCaseDraft[]` snapshot diff instead of a sticky boolean, which removes false-positive confirm prompts after the user reverts spec changes. Exposed validation-contract constants for frontend quick-guard vs backend canonical normalization, added route and UI regression coverage around that boundary, and introduced fixture-driven builder goldens for GPS device issuance, fire-incident response, and a simple swimlane-theme feature. Synced UC-07, changelog, task statuses, and reran targeted backend tests, UI tests, Playwright mock E2E, and production build successfully.
- Files: `src/App.tsx`, `src/usecases/prevalidate.ts`, `src/usecases/prevalidate.test.ts`, `src/usecases/UseCasePanel.tsx`, `src/usecases/UseCasePanel.test.tsx`, `e2e/brd-flow.spec.ts`, `apps/api/tests/test_usecase_routes.py`, `apps/api/tests/test_usecase_builder.py`, `apps/api/tests/fixtures/usecase_generation/*.json`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: The next clean handoff point is `TASK-075`, because the upstream `spec -> use case` layer now has a much steadier review contract and more believable segmentation coverage.
- Status: Done

### 2026-06-04 20:32 - Review implementation quality of TASK-083 to TASK-085

- Category: review
- Request: Review the newly implemented `TASK-083` to `TASK-085`, focusing on snapshot-derived overwrite protection, the frontend/backend validation contract, and the new domain-grade segmentation fixtures.
- Action: Inspected the updated `App.tsx` review-state flow, frontend prevalidation helpers, builder fixture tests, route normalization tests, and E2E coverage. Confirmed the batch is a solid improvement with no new blocking defects, but identified three follow-up hardening items: the validation contract is still duplicated manually across frontend/backend, the new domain fixtures remain shallow for the quality bar implied by “goldens”, and the new snapshot-based draft dirty-state is only explicitly regression-tested for spec reverts, not reverted draft edits. Captured a review snapshot and added follow-up tasks `TASK-086` to `TASK-088`.
- Files: `src/App.tsx`, `src/usecases/prevalidate.ts`, `src/usecases/UseCasePanel.tsx`, `apps/api/tests/test_usecase_builder.py`, `apps/api/tests/test_usecase_routes.py`, `e2e/brd-flow.spec.ts`, `docs/reviews/2026-06-04-task-083-085-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: `TASK-086` and `TASK-087` are the most leverage-heavy before building `UseCaseDraft -> DiagramDraft`; `TASK-088` is a smaller UX guardrail worth batching when the panel next changes.
- Status: Done

### 2026-06-04 20:39 - Explain the meaning of `Use Case Drafts` fields and whether they are system inputs

- Category: decision
- Request: Clarify what each field in `Use Case Drafts` means and whether that panel is the system input.
- Action: Reviewed the current frontend types and UC-07 workflow, then explained the artifact roles clearly: `ProjectSpec` and `FeatureIntent` are the real upstream inputs to the generation API, while `UseCaseDraft[]` is the generated intermediate artifact that users review/edit/approve before moving on to diagram generation and formal BRD synthesis.
- Files: `src/usecases/types.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/activity-log/2026-06.md`
- Follow-up: None required; this was a clarification of the current contract and workflow.
- Status: Decision

### 2026-06-04 20:42 - Clarify where to enter `ProjectSpec` and `FeatureIntent`

- Category: decision
- Request: Clarify exactly where the user should enter `ProjectSpec` and `FeatureIntent` in the current UI.
- Action: Confirmed that both artifacts are entered in the `Use Case Drafts` panel: the `Project spec` section at the top and the `Feature intent` section immediately below it, opened from the toolbar button `Use case drafts`.
- Files: `src/App.tsx`, `src/usecases/UseCasePanel.tsx`, `docs/activity-log/2026-06.md`
- Follow-up: None required; this was a direct UX clarification.
- Status: Decision

### 2026-06-04 20:49 - Review and define redesign tasks for the `Use case drafts` UX

- Category: review
- Request: Review the current `Use case drafts` UI/UX from the perspective that the user only cares about inputting project spec + function intent, receiving an editable use-case list as output, and then seeing how diagrams are listed per use case.
- Action: Reviewed the current `UseCasePanel`, editor shell, UC-07 workflow doc, and backlog state for `DiagramDraft`. Confirmed that the current panel mixes input, generated output, and architecture/debug context into one long scroll surface, while the next artifact (`DiagramDraft`) is still only implied in docs/backlog and not represented in the UI. Captured a dedicated UX review snapshot and added redesign tasks for splitting the workspace into `Input / Use cases / Diagrams`, renaming the workspace, hiding `Artifact chain` from the primary flow, introducing a diagram inventory surface, and updating docs to the new mental model.
- Files: `src/usecases/UseCasePanel.tsx`, `src/App.tsx`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/reviews/2026-06-04-usecase-workspace-ux-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: The highest-leverage starting point is `TASK-089` and `TASK-092`, because they define the new UI skeleton and make `DiagramDraft` visible in the product instead of only in architecture docs.
- Status: Done

### 2026-06-04 20:55 - Implement the new `Use Case Workspace` UX shell

- Category: implementation
- Request: Implement `TASK-089` to `TASK-094`: split the old `Use case drafts` panel into `Input / Use cases / Diagrams`, rename the workspace, hide `Artifact chain` from the main path, add a diagram inventory per use case, surface next actions, and sync UC-07 plus roadmap docs.
- Action: Rebuilt the old single-scroll panel into a 3-zone `Use Case Workspace` with explicit tabs for `Input`, `Use cases`, and `Diagrams`. Moved artifact-chain details behind `Advanced traceability`, rewired review actions so approved items now expose a clear handoff into a new diagram inventory, and added lightweight frontend state for per-use-case diagram slots plus canvas handoff selection. Updated the editor toolbar label, workspace copy, use-case component tests, UC-07, roadmap notes, changelog, and backlog statuses. Verified with `npm run test:ui-mock`, `npm run test:api-mock`, `npm run test:e2e-mock`, and `npm run build`.
- Files: `src/App.tsx`, `src/usecases/UseCasePanel.tsx`, `src/usecases/types.ts`, `src/usecases/UseCasePanel.test.tsx`, `src/styles.css`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/roadmap/README.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: The natural next slice is `TASK-075`, so the new diagram inventory can point to real generated `DiagramDraft` artifacts instead of only the Phase 1 handoff contract.
- Status: Done

### 2026-06-05 00:11 - Review implementation quality of TASK-089 to TASK-094

- Category: review
- Request: Re-review the implemented `Use Case Workspace` redesign (`TASK-089` to `TASK-094`) and assess whether the new `Input / Use cases / Diagrams` flow is strong enough to support the next `UseCaseDraft -> DiagramDraft` step.
- Action: Reviewed the editor shell state, the new `UseCasePanel` flow, updated component/E2E coverage, and the refreshed UC-07 contract. Confirmed the redesign is directionally good and much closer to the user’s mental model, but identified three follow-up risks: approved use cases can still be edited without invalidating readiness, the `Open canvas` handoff loses persistent visible context once the workspace closes, and the primary UX copy is still mixed Vietnamese/English despite the repo’s VN-first contract. Captured a detailed review snapshot and added follow-up tasks `TASK-095` to `TASK-097`.
- Files: `src/App.tsx`, `src/usecases/UseCasePanel.tsx`, `src/usecases/types.ts`, `src/usecases/UseCasePanel.test.tsx`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/reviews/2026-06-05-task-089-094-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Prioritize `TASK-095` and `TASK-096` before landing real `UseCaseDraft -> DiagramDraft` generation, so the review boundary and canvas handoff are trustworthy.
- Status: Done

### 2026-06-05 10:34 - Implement TASK-095 to TASK-097 for use case review trust and VN-first UX

- Category: implementation
- Request: Implement `TASK-095` to `TASK-097`: invalidate approved use cases after content edits, preserve active-use-case context on the editor canvas, and normalize the workspace copy to VN-first UI.
- Action: Added an `approved -> reviewed` policy for content edits, blocked diagram handoff until the edited item is phê duyệt again, introduced a persistent editor-shell context chip with the active `use_case_id`, title, review/diagram state, and quick return to the diagram inventory, and translated the primary workspace headings, tabs, actions, statuses, and empty states to Vietnamese-first copy. Updated component/E2E regression coverage, UC-07, changelog, and task statuses. Verified with focused Vitest, `npm run build`, full Playwright mock E2E, and a browser visual pass.
- Files: `src/App.tsx`, `src/usecases/UseCasePanel.tsx`, `src/usecases/UseCasePanel.test.tsx`, `src/styles.css`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: `TASK-075` can now build the real `UseCaseDraft -> DiagramDraft` generation path on top of a trustworthy review boundary and visible canvas binding.
- Status: Done

### 2026-06-05 10:58 - Review implementation quality of TASK-095 to TASK-097

- Category: review
- Request: Review the implemented approval invalidation, persistent active-use-case canvas context, and VN-first copy changes from `TASK-095` to `TASK-097`.
- Action: Reviewed the editor state transitions, workspace action gating, lifecycle types, copy surface, component/E2E coverage, and UC-07 contract. Confirmed that edit-after-approve works and the context strip is visible, but found a P1 ambiguity between inventory focus and the use case actually active on canvas, a second lifecycle source-of-truth risk for `Mở canvas`, incomplete VN-first normalization, and duplicated lifecycle logic in tests. Saved a review snapshot, opened `KI-18`, and added follow-up tasks `TASK-098` to `TASK-101`.
- Files: `src/App.tsx`, `src/usecases/UseCasePanel.tsx`, `src/usecases/types.ts`, `src/usecases/UseCasePanel.test.tsx`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/reviews/2026-06-05-task-095-097-implementation-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: Complete `TASK-098` and `TASK-099` before `TASK-075`; then harden tests with `TASK-101` and finish the glossary with `TASK-100`.
- Status: Done
### 2026-06-05 12:12 - Recommend next steps after TASK-095 to TASK-097

- **Request:** Recommend the next implementation steps after reviewing TASK-095 through TASK-097.
- **Category:** Decision / planning
- **Action:** Recommended completing lifecycle hardening with TASK-098, TASK-099, and TASK-101 before starting real diagram generation; then handling TASK-100 as a separate VN-first copy pass, followed by TASK-075 and TASK-076.
- **Files reviewed:** `docs/reviews/2026-06-05-task-095-097-implementation-review.md`, `docs/review-task-list.md`, `docs/progress/README.md`
- **Follow-up:** Implement and verify the lifecycle-hardening batch, including focus-versus-active E2E coverage.
- **Status:** Decision

### 2026-06-05 12:39 - Implement TASK-098, TASK-099, and TASK-101 lifecycle hardening

- Category: implementation
- Request: Separate diagram inventory focus from the active canvas binding, establish one diagram lifecycle source of truth for status and `Mở canvas`, and extract lifecycle policy into tested pure helpers.
- Action: Added shared use-case lifecycle helpers, rewired the editor and inventory to distinguish focused versus active items, derived copy/style/action permission from diagram lifecycle, added field-level and blocked-state unit/component tests, and covered the A-active/B-focused/B-active transition in Playwright. Closed KI-18 and synchronized UC-07, changelog, and task statuses.
- Files: `src/usecases/lifecycle.ts`, `src/usecases/lifecycle.test.ts`, `src/usecases/types.ts`, `src/usecases/UseCasePanel.tsx`, `src/usecases/UseCasePanel.test.tsx`, `src/App.tsx`, `src/styles.css`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: TASK-075 can now consume the lifecycle contract; TASK-100 remains the next independent VN-first copy cleanup.
- Status: Done

### 2026-06-05 13:15 - Review the current use-case-to-diagram workflow

- Category: review
- Request: Explain how the current flow creates detailed use cases and diagrams, and investigate why `Mở canvas` still shows the initial sample diagram.
- Action: Traced the generation, review, lifecycle, and LogicFlow render paths. Confirmed that `Mở canvas` only binds the selected use case to the existing canvas and does not generate or load graph data. Also confirmed that the current use-case schema is summary-oriented and lacks per-step actor/branch trace structure needed for reliable diagram generation. Captured a review snapshot, opened KI-19, added TASK-102 and TASK-103, and made them prerequisites for TASK-075.
- Files: `src/App.tsx`, `src/lf-config.ts`, `src/usecases/*`, `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `docs/reviews/2026-06-05-current-usecase-to-diagram-flow-review.md`, `docs/progress/known-issues.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Execute TASK-103 for truthful CTA behavior, then TASK-102 and TASK-075 for real diagram generation; TASK-076 follows for round-trip traceability.
- Status: Done

### 2026-06-05 14:20 - Implement detailed use cases and real diagram generation

- Category: implementation
- Request: Implement TASK-103, TASK-102, TASK-075, and TASK-076 for truthful diagram CTAs, diagram-ready use cases, real graph generation, and trace preservation after edits.
- Action: Added structured main/alternate flow contracts, deterministic diagram generation with actor lanes and traceable nodes/edges, real LogicFlow rendering per use case, `Tạo sơ đồ` versus `Mở canvas` lifecycle semantics, and `outdated/diverged` handling with guarded regeneration. Fixed branch layout after Browser QA exposed overlapping exception paths.
- Files: `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/app/services/diagram_builder.py`, `apps/api/app/routes/diagram_generate.py`, `src/usecases/*`, `src/App.tsx`, `src/styles.css`, `e2e/brd-flow.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/scope/artifact-chain.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`
- Follow-up: Persist diagram workspaces beyond the frontend session and implement portfolio-level formal BRD generation when moving to the next roadmap slice.
- Status: Done

### 2026-06-06 10:00 - Review TASK-102, TASK-103, TASK-075, and TASK-076

- Category: review
- Request: Review the implemented detailed use-case contract, diagram CTA, real diagram generation, and round-trip trace behavior.
- Action: Reviewed the API contract/builders, editor approval flow, diagram lifecycle, LogicFlow mutation capture, Draw.io interchange, and test coverage. Confirmed the main generation flow works, but found P1 gaps in edit-time contract integrity, destructive regeneration, layout snapshot coverage, and terminal-flow semantics, plus P2 provenance/recovery gaps. Saved a review snapshot and added TASK-104 to TASK-109.
- Files: `apps/api/app/schemas/usecase.py`, `apps/api/app/services/diagram_builder.py`, `src/usecases/*`, `src/App.tsx`, `src/io/drawio-*`, `e2e/brd-flow.spec.ts`, `docs/reviews/2026-06-06-task-102-103-075-076-implementation-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`
- Follow-up: Implement TASK-104 through TASK-108 before relying on this artifact chain for portfolio BRD or persistence; TASK-109 should land before interchange becomes a formal round-trip path.
- Status: Done

### 2026-06-06 10:41 - Implement TASK-104 through TASK-109

- Category: implementation
- Request: Implement in order the detailed-contract guard, backend invariants, workspace recovery, unified canvas commits, terminal alternate outcomes, and provenance/trace round-trip tasks.
- Action: Added frontend/backend contract validation, preserved orphan/current diagram artifacts across regeneration, routed canvas mutations through a typed workspace commit path, rendered terminal outcomes separately from success, and introduced versioned node/edge provenance with Draw.io round-trip plus trace coverage. Closed KI-20 through KI-23 and synchronized UC-07 and the canonical artifact chain.
- Files: `src/usecases/*`, `src/io/*`, `src/App.tsx`, `src/styles.css`, `apps/api/app/schemas/usecase.py`, `apps/api/app/services/diagram_builder.py`, `apps/api/tests/*`, `e2e/brd-flow.spec.ts`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/scope/artifact-chain.md`
- Follow-up: Persist diagram workspaces and provenance beyond the frontend session before relying on them for multi-session formal BRD assembly.
- Status: Done

### 2026-06-06 15:20 - Review necessity of advanced use-case input fields

- Category: review
- Request: Explain why business context, business rules, and glossary are shown in the use-case workspace and assess whether they are excessive.
- Action: Traced each field from UI through normalization, API schema, builder, and tests. Confirmed all three are optional; business context overlaps project summary, business rules overlap feature constraints, and glossary has no generation consumer. Recommended an essential-first form, opened KI-24, and added TASK-110/TASK-111.
- Files: `src/usecases/UseCasePanel.tsx`, `src/usecases/prevalidate.ts`, `src/App.tsx`, `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/tests/test_usecase_builder.py`, `docs/reviews/2026-06-06-usecase-input-field-necessity-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`
- Follow-up: Implement TASK-110 before adding more fields to the use-case intake.
- Status: Done

### 2026-06-06 15:23 - Audit every field in the use-case workspace

- Category: review
- Request: Review all fields across the use-case workspace and identify redundant or unnecessarily prominent inputs.
- Action: Audited all 17 intake fields plus the generated use-case editor against builder and diagram consumers. Identified dead fields (`function_name`, `glossary`), overlapping input concepts, duplicate editable summary/structured flow models, incomplete alternate-flow editing, and excessive technical metadata. Expanded TASK-110/TASK-111 and added TASK-112/TASK-113.
- Files: `src/usecases/UseCasePanel.tsx`, `src/usecases/diagram.ts`, `src/usecases/contract.ts`, `src/usecases/prevalidate.ts`, `apps/api/app/services/usecase_builder.py`, `apps/api/app/services/diagram_builder.py`, `docs/reviews/2026-06-06-usecase-workspace-field-audit.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`
- Follow-up: Implement TASK-110 first, then TASK-112; finish contract cleanup and progressive disclosure with TASK-111/TASK-113.
- Status: Done

### 2026-06-06 16:37 - Implement essential-first use-case workspace

- Category: implementation
- Request: Implement TASK-110, TASK-112, TASK-111, and TASK-113 in the requested order.
- Action: Simplified intake to essential fields with compatibility adapters, made structured main/alternate flows canonical, documented enrichment consumers, added progressive disclosure, and fixed mobile workspace overflow found during Browser QA.
- Files: `src/usecases/*`, `src/App.tsx`, `src/styles.css`, `apps/api/tests/*`, `e2e/brd-flow.spec.ts`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/scope/artifact-chain.md`
- Follow-up: [Task list](../review-task-list.md), [UC-07](../use-cases/UC-07-sinh-usecase-tu-spec.md), [changelog](../progress/changelog.md)
- Status: Done

### 2026-06-06 16:58 - Explain current use-case generation logic

- Category: question
- Request: Explain the execution flow and decision logic that creates use cases from the current workspace input.
- Action: Traced frontend validation/canonicalization, the deterministic API builder, use-case segmentation rules, structured flow construction, and the post-generation review lifecycle. Clarified that generation currently uses rule-based templates rather than an LLM.
- Files: `src/App.tsx`, `src/usecases/prevalidate.ts`, `src/usecases/editor.ts`, `apps/api/app/routes/usecase_generate.py`, `apps/api/app/schemas/usecase.py`, `apps/api/app/services/usecase_builder.py`
- Follow-up: [UC-07](../use-cases/UC-07-sinh-usecase-tu-spec.md)
- Status: Decision

### 2026-06-06 17:06 - Review whether use cases are AI-generated

- Category: review
- Request: Verify whether current use cases are generated by AI and explain why their content is generic.
- Action: Confirmed the endpoint uses a deterministic keyword/template builder rather than OpenRouter, documented the expectation and quality gap as KI-25, and added TASK-114 through TASK-117 for truthful source labeling, a shared provider boundary, guarded AI synthesis, and quality evaluation.
- Files: `apps/api/app/routes/usecase_generate.py`, `apps/api/app/services/usecase_builder.py`, `apps/api/app/providers/openrouter_provider.py`, `docs/reviews/2026-06-06-usecase-generation-quality-review.md`, `docs/progress/known-issues.md`, `docs/review-task-list.md`
- Follow-up: Implement TASK-114 first, then TASK-115 → TASK-116 → TASK-117.
- Status: Done

### 2026-06-06 17:18 - Locate current AI prompts

- Category: question
- Request: Identify where prompts are stored and which generation paths currently use them.
- Action: Confirmed that the only model prompt is built inline in `apps/api/app/services/prompt_builder.py` for BRD generation; dynamic user content is assembled per request and sent to OpenRouter without a prompt registry or persistence layer. Use-case and diagram generation currently have no AI prompts.
- Files: `apps/api/app/services/prompt_builder.py`, `apps/api/app/routes/brd_generate.py`, `apps/api/app/providers/openrouter_provider.py`, `apps/api/app/config.py`
- Follow-up: TASK-115 and TASK-116 should introduce a shared provider boundary and a separate versioned use-case prompt.
- Status: Decision

### 2026-06-06 17:30 - Plan structured AI use-case generation

- Category: planning
- Request: Create executable tasks for clean code structure, standardized prompts, and AI-generated use cases.
- Action: Expanded the AI use-case backlog into a staged architecture covering shared provider/config, owned generation modules, versioned prompt registry, semantic synthesis schema, deterministic ID hydration, grounding/injection guards, orchestration with fallback, observability, quality gates, and feature-flagged rollout.
- Files: `docs/reviews/2026-06-06-usecase-generation-quality-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement in the documented order starting with TASK-114 and TASK-115.
- Status: Done

### 2026-06-06 17:39 - Implement guarded AI use-case generation

- Category: implementation
- Request: Implement TASK-114, TASK-115, TASK-118, TASK-119, TASK-120, TASK-121, TASK-116, TASK-122, TASK-117, and TASK-123.
- Action: Added a shared provider/config platform, versioned prompt registry, semantic synthesis and deterministic hydration, grounding/injection guards, quality gates, retry/fallback orchestration, source/cost metadata, and deterministic/shadow/opt-in/default rollout modes. Added truthful source controls and labels in the workspace.
- Files: `apps/api/app/ai/*`, `apps/api/app/usecases/*`, `apps/api/app/routes/*`, `apps/api/app/schemas/*`, `src/usecases/*`, `src/App.tsx`, `src/styles.css`, tests, `README.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/*`
- Follow-up: Keep production on `deterministic` or `ai_opt_in` until ongoing quality, fallback-rate, latency, and cost thresholds justify `ai_default`.
- Status: Done

### 2026-06-06 17:49 - Review TASK-114 through TASK-123 implementation

- Category: review
- Request: Review implemented TASK-114 through TASK-123 for AI-backed use-case generation.
- Action: Created an implementation review snapshot, identified grounding/quality, prompt fallback, mock provider, and rollout-eval gaps, added executable follow-up tasks, and opened known issues for confirmed defects.
- Files: `docs/reviews/2026-06-06-task-114-123-implementation-review.md`, `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement TASK-124 through TASK-127 before considering `ai_default`.
- Status: Done

### 2026-06-06 17:56 - Clarify multi-actor input for AI use-case generation

- Category: decision
- Request: Clarify whether AI can detect multiple actors in a use case and where users should enter required actors.
- Action: Reviewed the use-case prompt, grounding guard, backend schemas, frontend input panel, and canonical request builder; confirmed AI is constrained to actors/systems present in input and users should fill the primary actor plus additional participants/systems in the use-case workspace.
- Files: `apps/api/app/ai/prompts/registry.py`, `apps/api/app/usecases/grounding.py`, `apps/api/app/schemas/usecase.py`, `src/usecases/UseCasePanel.tsx`, `src/usecases/prevalidate.ts`, `docs/activity-log/2026-06.md`
- Follow-up: Consider making the participants field more visible if multi-actor workflows are common.
- Status: Decision

### 2026-06-06 17:57 - Clarify exact actor input location

- Category: decision
- Request: Clarify the exact UI location where users enter actors for use-case generation.
- Action: Answered directly that the primary actor is entered in `Actor chính`, while additional actors/systems are entered under `Thông tin bổ sung` in `Bên tham gia hoặc hệ thống`.
- Files: `src/usecases/UseCasePanel.tsx`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Decision

### 2026-06-06 18:02 - Define actor input correction task

- Category: planning
- Request: Stop splitting actors into primary/supporting, remove the `Thông tin bổ sung` actor input pattern, and create a task to fix the workflow.
- Action: Added TASK-128 to replace `Actor chính` with a primary `Actors / swimlanes` list, remove `Thông tin bổ sung` from the generate input, keep backend compatibility hidden, and update docs/tests. Updated UC-07 and artifact-chain docs to record the product decision that all user-entered actors are peer actors.
- Files: `docs/review-task-list.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/scope/artifact-chain.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement TASK-128.
- Status: Done

### 2026-06-06 18:10 - Implement TASK-128 actor input simplification

- Category: implementation
- Request: Implement TASK-128.
- Action: Replaced the use-case input actor split with one primary `Actors / swimlanes` multiline field, removed `Thông tin bổ sung` from the generate input, surfaced the remaining generation inputs directly, rendered generated actors as one combined `Actors` field in review, and kept backend compatibility mapping hidden. Updated prompt wording, docs, changelog, task status, and tests.
- Files: `src/usecases/UseCasePanel.tsx`, `src/usecases/prevalidate.ts`, `src/usecases/UseCasePanel.test.tsx`, `src/usecases/prevalidate.test.ts`, `e2e/brd-flow.spec.ts`, `apps/api/app/ai/prompts/registry.py`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/activity-log/2026-06.md`
- Follow-up: Consider a deeper backend schema migration from `primary_actor/supporting_actors` to a first-class `actors` field after compatibility needs are settled.
- Status: Done

### 2026-06-06 18:28 - Redesign project context input

- Category: implementation
- Request: Make the `Bối cảnh dự án` input clearer and better designed.
- Action: Replaced the collapsed project-context disclosure with an always-visible `ProjectSpec` panel, clearer labels/placeholders, responsive desktop/mobile layout, and regression coverage.
- Files: `src/usecases/UseCasePanel.tsx`, `src/styles.css`, `src/usecases/UseCasePanel.test.tsx`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-06 19:00 - Fix actor textarea Enter input

- Category: bug
- Request: Fix `Actors / swimlanes` so users can press Enter to enter multiple actors.
- Action: Confirmed the controlled textarea was normalizing away trailing blank lines, added raw input state for the actor textarea while preserving canonical actor normalization, and added regression coverage plus Browser QA.
- Files: `src/usecases/UseCasePanel.tsx`, `src/usecases/UseCasePanel.test.tsx`, `docs/progress/changelog.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-06 19:24 - Fix shape text auto-wrap

- Category: bug
- Request: Improve UI/UX so text automatically wraps within shape limits.
- Action: Enabled LogicFlow auto-wrap for activity, decision, and note nodes; preserved manual newlines; added break-word wrapping and pure text sizing helpers with regression coverage. Verified long sample texts in Browser do not overflow their text box.
- Files: `src/node-text.ts`, `src/nodes.ts`, `src/nodes.test.ts`, `docs/progress/changelog.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-06 19:32 - Fix lane auto-centering overlap

- Category: bug
- Request: Fix shapes auto-centering in lanes because it causes overlapping shapes.
- Action: Changed DnD/drop placement to bind shapes to the nearest lane while preserving the user's horizontal position, clamping only at lane boundaries. Updated sidebar guidance and added a Playwright regression for dropped and moved activity nodes.
- Files: `src/App.tsx`, `src/DndPanel.tsx`, `e2e/brd-flow.spec.ts`, `docs/progress/changelog.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-06 20:50 - Fix shape drag from node text

- Category: bug
- Request: Make dragging from text inside a shape move the shape instead of treating the label as a separate drag target.
- Action: Disabled independent node-label dragging while preserving edge-label dragging and double-click text editing; added unit and browser regression coverage.
- Files: `src/lf-config.ts`, `src/lf-config.test.ts`, `e2e/brd-flow.spec.ts`, `docs/progress/changelog.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-06 21:24 - Add Clerk authentication

- Category: implementation
- Request: Configure Clerk authentication through the Clerk CLI and expose clear account controls in the app.
- Action: Installed and authenticated Clerk CLI, linked the Smart Diagram application, pulled ignored development keys, installed the React SDK and Clerk agent skills, added the root provider plus sign-in/sign-up/user controls, polished the wrapping header layout, and verified both auth modals in Browser.
- Files: `package.json`, `package-lock.json`, `src/main.tsx`, `src/vite-env.d.ts`, `src/App.tsx`, `src/styles.css`, `.agents/skills/*`, `skills-lock.json`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Complete the first test-user signup in the open Clerk dialog and confirm the user profile button appears.
- Status: Partial

### 2026-06-07 00:50 - Design current project database

- Category: review
- Request: Design persistence for the current Swimlane Activity Diagram project's full artifact chain.
- Action: Corrected an initial scope misread, removed the unrelated Smart Diagram artifacts, and designed PostgreSQL persistence for Clerk users/workspaces, projects, ProjectSpec/FeatureIntent, use-case approvals, diagram working copies/revisions, BRD revisions, generation provenance, idempotency, audit, and sharing. Updated the Phase 2 persistence decision and added executable `TASK-129` through `TASK-136`.
- Files: `docs/scope/database-architecture.md`, `docs/reviews/2026-06-07-current-project-database-design-review.md`, `docs/scope/architecture.md`, `docs/roadmap/phase-2-collaboration.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Start with backend Clerk JWT verification and PostgreSQL/Alembic foundation.
- Status: Done

### 2026-06-07 01:25 - Simplify database and Save UX scope

- Category: planning
- Request: Reduce the database to latest-state user/project/spec/feature/use-case/diagram/BRD tables and add explicit Save actions with a matching UI flow.
- Action: Replaced the revision/workspace design with a seven-table ownership chain, reserved only `user/admin` roles, documented latest-state CRUD and cascade rules, redesigned the UI around project selection plus separate Spec and Feature sections, and rewrote the persistence implementation direction around explicit Save.
- Files: `docs/scope/database-architecture.md`, `docs/reviews/2026-06-07-current-project-database-design-review.md`, `docs/scope/artifact-chain.md`, `docs/roadmap/phase-2-collaboration.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-129` and `TASK-130` first.
- Status: Done

### 2026-06-07 11:48 - Review Railway database implementation readiness

- Category: review
- Request: Review the provided Railway PostgreSQL environment and expand the implementation backlog for the revised backend/frontend persistence flow.
- Action: Confirmed the public proxy is reachable, identified credential rotation, unresolved local Railway references, missing persistence/auth/deploy foundations, CORS and resource-identity risks, then expanded the implementation plan into sequenced backend/frontend tasks `TASK-129` through `TASK-147`.
- Files: `docs/scope/database-architecture.md`, `docs/reviews/2026-06-07-current-project-database-design-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Rotate the database credential, then execute `TASK-129` through `TASK-134` before artifact CRUD.
- Status: Done

### 2026-06-07 11:50 - Clarify database implementation sequence

- Category: decision
- Request: Summarize the database implementation tasks and state the correct execution order.
- Action: Confirmed the 19-task backlog and clarified the dependency-driven order: secrets and persistence foundation first, then deploy/auth/ownership/client foundations, followed by backend-to-frontend vertical slices for Project/Spec, FeatureIntent, UseCase, Diagram and BRD, ending with shared Save guards, full-chain tests and Railway operations.
- Files: `docs/review-task-list.md`, `docs/reviews/2026-06-07-current-project-database-design-review.md`, `docs/activity-log/2026-06.md`
- Follow-up: Begin with `TASK-129`, then `TASK-131`; complete `TASK-130` once Alembic exists.
- Status: Decision

### 2026-06-07 12:20 - Implement latest-state database and project persistence flow

- Category: implementation
- Request: Implement `TASK-129` through `TASK-147` for the revised Project/Spec/Feature/UseCase/Diagram/BRD flow.
- Action: Added PostgreSQL SQLAlchemy models and Alembic migration, Clerk backend auth and ownership guards, resource-scoped CRUD/generation APIs, Railway runtime/readiness contract, protected project routing, dashboard and Spec/Feature workspace, explicit Use Case/Diagram/BRD Save/Load states, unsaved-change guards, full-chain PostgreSQL ownership test, and Railway release/backup runbook. Verified local migration round-trip, backend/frontend suites, production build and signed-out browser routing. Railway credential rotation, staging deployment, backup drill and authenticated Clerk browser E2E remain externally blocked or follow-up work and are marked Partial.
- Files: `apps/api/pyproject.toml`, `apps/api/alembic.ini`, `apps/api/alembic/*`, `apps/api/app/{auth,config,db,main,models,persistence}.py`, `apps/api/app/routes/persistence.py`, `apps/api/app/schemas/{persistence,usecase}.py`, `apps/api/tests/*`, `src/application/*`, `src/persistence/*`, `src/App.tsx`, `src/main.tsx`, `src/styles.css`, `src/usecases/*`, `src/brd/BrdPanel.tsx`, `railway.toml`, `docs/operations/railway-persistence-release.md`, `docs/review-task-list.md`, `docs/task-implementation-plan.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-authenticate/link Railway, rotate the exposed PostgreSQL credential, deploy staging, run the documented backup/restore drill, then add the authenticated Clerk Playwright matrix and remaining per-scope navigation guards.
- Status: Partial

### 2026-06-07 12:36 - Review TASK-131 to TASK-145 implementation

- Category: review
- Request: Review the implemented `TASK-131` through `TASK-145` work and plan next implementation steps.
- Action: Reviewed runtime/deploy, auth, schema, persistence routes, frontend project workspace, editor Save integration and tests. Confirmed the API mock script currently fails with system Python missing `clerk_backend_api`, identified persisted-workspace fake editable fields, save-state leakage across scopes, shallow diagram payload validation and auth test gaps. Added a review snapshot and new follow-up tasks `TASK-148` through `TASK-158`.
- Files: `docs/reviews/2026-06-07-task-131-145-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Start with `TASK-148`, `TASK-149`, `TASK-150`, `TASK-151`, and `TASK-152` before adding more product functionality.
- Status: Done

### 2026-06-07 14:52 - Implement persistence hardening tasks 148 to 158

- Category: implementation
- Request: Implement `TASK-148` through `TASK-158` after the persistence review.
- Action: Standardized backend scripts on `apps/api/.venv/bin/python`, added API smoke command, Clerk auth matrix tests, saved diagram graph validation, persistence serializer/service/generation modules, persisted UseCasePanel read-only parent context, scoped Save-state handling, explicit UseCase delete, scoped BRD recovery cache, active feature deep-link route, scenario matrix tests, and Docker API build/health rehearsal. Fixed Docker package discovery by excluding Alembic from setuptools package discovery.
- Files: `package.json`, `README.md`, `apps/api/pyproject.toml`, `apps/api/app/auth.py`, `apps/api/app/schemas/persistence.py`, `apps/api/app/routes/persistence.py`, `apps/api/app/services/persistence_*.py`, `apps/api/tests/test_persistence_auth_matrix.py`, `src/application/*`, `src/persistence/*`, `src/usecases/UseCasePanel.tsx`, `src/usecases/UseCasePanel.test.tsx`, `src/brd/cache.ts`, `src/brd/cache.test.ts`, `src/App.tsx`, `src/styles.css`, `docs/review-task-list.md`, `docs/task-implementation-plan.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-authenticate Railway CLI with `railway login`, run `railway link`, then verify staging migration, `/readyz`, and live deploy health.
- Status: Partial

### 2026-06-07 15:20 - Review persistence hardening tasks 148 to 158

- Category: review
- Request: Review the implemented `TASK-148` through `TASK-158` work.
- Action: Reviewed backend runner/package build, Clerk auth matrix, persistence service extraction, diagram validation, ProjectWorkspace scoped Save state, persisted UseCasePanel mode, BRD scoped cache, routing and operation verification. Confirmed backend/UI tests pass, corrected stale `TASK-148` status, and added follow-up tasks `TASK-159` through `TASK-163` for inactive dirty-state tracking, guarded diagram switches, invalid feature deep-link UX, BRD load error handling and Railway staging rehearsal.
- Files: `docs/reviews/2026-06-07-task-148-158-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-159` before more persisted-workspace UX work.
- Status: Done

### 2026-06-07 15:36 - Implement persistence hardening tasks 159 to 163

- Category: implementation
- Request: Implement `TASK-159` through `TASK-163`.
- Action: Added a persisted Save-state registry and unit tests, wired aggregate dirty-state guards across inactive resource scopes, guarded diagram/use-case canvas switches with scoped dirty prompts, made invalid feature deep-links explicit, added persisted BRD load error handling that preserves recovery drafts, updated task/changelog/implementation docs, and rechecked Railway CLI status.
- Files: `src/persistence/save-state.ts`, `src/persistence/save-state.test.ts`, `src/persistence/WorkspaceContext.tsx`, `src/application/ProjectWorkspace.tsx`, `src/App.tsx`, `docs/review-task-list.md`, `docs/task-implementation-plan.md`, `docs/progress/changelog.md`, `docs/operations/railway-persistence-release.md`, `docs/activity-log/2026-06.md`
- Follow-up: Complete `TASK-163` after the project owner runs `railway login` and `railway link`.
- Status: Partial

### 2026-06-07 15:43 - Review persistence hardening tasks 159 to 163

- Category: review
- Request: Review the implemented `TASK-159` through `TASK-163` work.
- Action: Reviewed save-state registry, ProjectWorkspace routing/dirty guards, diagram switch flow, BRD recovery load handling and Railway runbook. Confirmed build/UI tests pass, Railway remains blocked by CLI auth/link, and added follow-up tasks `TASK-164` through `TASK-168` for stale scope cleanup, invalid route stale context, transactional diagram load, BRD recovery conflict policy and regression coverage.
- Files: `docs/reviews/2026-06-07-task-159-163-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-164`, `TASK-165` and `TASK-166` before treating persisted workspace Save UX as stable.
- Status: Done

### 2026-06-07 15:48 - Review Clerk backend config runtime blocker

- Category: review
- Request: Check whether the existing task list covers the runtime error `Clerk backend authentication is not configured.`
- Action: Traced the error to missing Clerk backend auth variables in `apps/api/.env`, confirmed existing auth tasks cover implementation/tests but not local config preflight, documented `KI-32`, and added `TASK-169` for Clerk backend config preflight and local auth setup guard.
- Files: `apps/api/app/auth.py`, `apps/api/app/config.py`, `apps/api/.env`, `docs/progress/known-issues.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-169`; for immediate local unblock choose either real Clerk mode with backend key or fast dev mode with `AUTH_DISABLED=true`.
- Status: Done

### 2026-06-07 15:59 - Update Clerk config task after env keys added

- Category: review
- Request: Update the task plan after adding `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` to fix `Clerk backend authentication is not configured.`
- Action: Verified a fresh backend Python process sees `CLERK_SECRET_KEY` without printing secrets, identified that the Vite frontend uses `VITE_CLERK_PUBLISHABLE_KEY` rather than `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, and updated `TASK-169` plus `KI-32` to focus on auth preflight, restart guidance and env-name drift.
- Files: `src/main.tsx`, `apps/api/app/auth.py`, `apps/api/.env`, `.env.local`, `docs/progress/known-issues.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-169`; restart the FastAPI backend after editing `apps/api/.env` before re-testing authenticated routes.
- Status: Done

### 2026-06-07 16:03 - Clarify Clerk frontend and backend env names

- Category: decision
- Request: Confirm whether the Clerk publishable key must use `VITE_CLERK_PUBLISHABLE_KEY`.
- Action: Confirmed the Vite frontend reads `VITE_CLERK_PUBLISHABLE_KEY` while the FastAPI backend independently reads `CLERK_SECRET_KEY`; clarified their expected env file scopes and restart requirements.
- Files: `src/main.tsx`, `apps/api/app/config.py`, `docs/activity-log/2026-06.md`
- Follow-up: Keep the publishable key in root `.env.local`, keep the secret key in `apps/api/.env`, then restart both dev processes.
- Status: Decision

### 2026-06-07 16:05 - Confirm Clerk secret env scope

- Category: decision
- Request: Confirm whether `CLERK_SECRET_KEY` is needed in root `.env.local`.
- Action: Confirmed the Vite frontend does not need the Clerk secret; it belongs only in the backend environment such as `apps/api/.env` and must never use a `VITE_` prefix.
- Files: `.env.local`, `apps/api/.env`, `src/main.tsx`, `apps/api/app/config.py`, `docs/activity-log/2026-06.md`
- Follow-up: Remove `CLERK_SECRET_KEY` from root `.env.local` and retain it only in the backend environment.
- Status: Decision

### 2026-06-07 16:18 - Implement persisted workspace transition hardening

- Category: implementation
- Request: Implement `TASK-164` through `TASK-168`.
- Action: Added feature-owned Save scopes and explicit cleanup, cleared stale editor context for invalid feature routes, made diagram load/generation switching transactional and guarded, protected dirty BRD recovery from server overwrite with an explicit server-version action, fixed BRD load retry, and added ProjectWorkspace/save-state/cache/BRD panel regressions.
- Files: `src/persistence/save-state.ts`, `src/persistence/save-state.test.ts`, `src/persistence/WorkspaceContext.tsx`, `src/application/ProjectWorkspace.tsx`, `src/application/ProjectWorkspace.test.tsx`, `src/brd/cache.ts`, `src/brd/cache.test.ts`, `src/brd/types.ts`, `src/brd/BrdPanel.tsx`, `src/brd/BrdPanel.test.tsx`, `src/App.tsx`, `docs/review-task-list.md`, `docs/task-implementation-plan.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-169` for Clerk config preflight; real hosted Clerk persistence E2E remains environment-backed work.
- Status: Done

### 2026-06-07 16:20 - Document database migration procedure

- Category: decision
- Request: Provide instructions for migrating the current PostgreSQL database.
- Action: Verified the Alembic configuration, confirmed migration head `20260607_01`, confirmed local `DATABASE_URL` uses the resolved Railway public proxy, and documented safe current/upgrade/verify/rollback commands.
- Files: `apps/api/alembic.ini`, `apps/api/alembic/env.py`, `apps/api/alembic/versions/20260607_01_latest_state.py`, `package.json`, `railway.toml`, `docs/activity-log/2026-06.md`
- Follow-up: Back up the target database, run `npm run db:migrate`, then verify `alembic current` reports `20260607_01 (head)`.
- Status: Decision

### 2026-06-07 16:30 - Fix Project Spec list input Space and Enter

- Category: bug
- Request: Fix `Người dùng mục tiêu` in Project Spec not accepting spaces or new lines.
- Action: Reworked the shared `ListField` to preserve raw textarea input while emitting a normalized string list, added a direct ProjectWorkspace regression for trailing space and Enter, and documented the confirmed defect as fixed.
- Files: `src/application/ProjectWorkspace.tsx`, `src/application/ProjectWorkspace.test.tsx`, `docs/progress/known-issues.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: None; the shared fix also applies to Business rules, Glossary and Feature list fields.
- Status: Done

### 2026-06-07 16:59 - Plan artifact tree and real-data-only workspace

- Category: review
- Request: Redesign the project workspace around a left artifact tree and remove all user-visible
  hardcoded sample Diagram/data.
- Action: Reviewed workspace routing, persistence loading, editor bootstrap, sample reset/demo paths,
  and E2E coupling. Documented the canonical tree UX and added `TASK-170` through `TASK-178` for the
  tree read contract, artifact routes, sidebar, editor integration, sample removal, truthful states,
  dirty guards, and regression migration.
- Files: `docs/reviews/2026-06-07-artifact-tree-real-data-ui-review.md`,
  `docs/scope/artifact-chain.md`, `docs/use-cases/UC-08-dieu-huong-artifact-tree.md`,
  `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-170` and `TASK-171` before building the sidebar UI.
- Status: Done

### 2026-06-07 17:34 - Implement artifact tree and real-data-only workspace

- Category: implementation
- Request: Implement `TASK-170` through `TASK-178`.
- Action: Added the owned artifact-tree API, canonical artifact deep-links, accessible left tree,
  persisted editor integration, truthful states and scoped dirty navigation; removed user-visible
  sample runtime and migrated editor coverage to a gated test harness plus a real full-chain E2E.
- Files: `apps/api/app/{routes,schemas,services}/persistence*`, `src/application/*`,
  `src/persistence/*`, `src/App.tsx`, `src/test-fixtures/*`, `src/test-harness/*`, `e2e/*`,
  `docs/review-task-list.md`, `docs/task-implementation-plan.md`, `docs/progress/changelog.md`
- Follow-up: Hosted Clerk/Railway verification remains tracked separately by existing infra tasks.
- Status: Done

### 2026-06-07 20:18 - Plan Feature Intent to Use Case sidebar flow

- Category: review
- Request: Create implementation tasks for the new UX where Feature Intent generation creates a Use Case list in the left bar, each Use Case is editable, and each approved Use Case can generate its own diagram.
- Action: Reviewed the current artifact tree, persisted workspace, use-case panel, and diagram handoff code; documented the remaining mismatch with the old panel/canvas workflow; added `TASK-179` through `TASK-183`; and updated UC-07 to make the left-bar persisted flow canonical.
- Files: `docs/reviews/2026-06-07-feature-intent-usecase-sidebar-flow-review.md`, `docs/review-task-list.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/activity-log/2026-06.md`
- Follow-up: Implement `TASK-179` before splitting the single Use Case editor from the old overlay.
- Status: Done

### 2026-06-07 22:05 - Implement persisted Feature Intent to Use Case to Diagram flow

- Category: implementation
- Request: Implement `TASK-179` through `TASK-183`.
- Action: Added a persisted `Use Cases` surface that generates and saves Use Case resources in one step, refreshes the left artifact tree immediately, splits Use Case list/editor routes out of the old canvas overlay, moves diagram creation onto approved Use Cases and missing-Diagram states, removes the old persisted `Không gian use case` workflow as a primary path, and updates regression coverage plus canonical docs for the shipped flow.
- Files: `src/application/ProjectWorkspace.tsx`, `src/application/ProjectWorkspace.test.tsx`, `src/usecases/PersistedUseCaseWorkspace.tsx`, `src/usecases/PersistedUseCaseWorkspace.test.tsx`, `src/App.tsx`, `src/persistence/WorkspaceContext.tsx`, `src/persistence/save-state.ts`, `src/persistence/save-state.test.ts`, `src/styles.css`, `e2e/artifact-tree.spec.ts`, `README.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: Re-run the broader BRD/playwright suite if we decide to migrate the older standalone `UseCasePanel` scenarios to the new persisted flow as well.
- Status: Done

### 2026-06-07 22:58 - Review implemented TASK-179 to TASK-183

- Category: review
- Request: Re-review the implementation of `TASK-179` through `TASK-183`.
- Action: Reviewed the persisted workspace shell, new `PersistedUseCaseWorkspace`, save-state/persistence contract, backend bulk-save behavior, and the new route/E2E/docs layer. Logged four follow-up issues: additive regenerate semantics in backend bulk save, missing retry-to-save recovery after generate success/save failure, missing persisted `diverged` diagram lifecycle parity plus ambiguous post-generate canvas handoff, and brittle BRD subtree assertions in the new artifact-tree E2E.
- Files: `docs/reviews/2026-06-07-task-179-183-implementation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`, `src/application/ProjectWorkspace.tsx`, `src/usecases/PersistedUseCaseWorkspace.tsx`, `src/persistence/WorkspaceContext.tsx`, `src/persistence/api.ts`, `apps/api/app/services/persistence_service.py`, `e2e/artifact-tree.spec.ts`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`
- Follow-up: Prioritize `TASK-184`, `TASK-185`, and `TASK-186` before treating the persisted Use Case flow as fully closed.
- Status: Done

### 2026-06-07 23:12 - Review persisted Use Case presentation quality

- Category: review
- Request: Review why AI-generated Use Cases on the persisted route are visually cluttered and propose a concrete solution path.
- Action: Reviewed the persisted Use Case route, shared `usecase-card` CSS primitives, legacy `UseCasePanel` presentation reuse, and current test coverage. Documented the core presentation issues: the route is still a raw form stack instead of a read-first artifact page, shared card styles are being stretched past their design boundary, step rows mix content and controls too densely, and there is no visual regression coverage for readability. Added follow-up tasks for IA redesign, dedicated layout primitives, progressive disclosure, and browser-level visual checks.
- Files: `docs/reviews/2026-06-07-usecase-presentation-review.md`, `docs/review-task-list.md`, `docs/activity-log/2026-06.md`, `src/usecases/PersistedUseCaseWorkspace.tsx`, `src/styles.css`, `src/usecases/UseCasePanel.tsx`, `src/usecases/PersistedUseCaseWorkspace.test.tsx`
- Follow-up: Execute `TASK-188` and `TASK-189` first; they are the structural fixes that make the rest of the UI cleanup coherent.
- Status: Done

### 2026-06-07 23:59 - Implement TASK-184 to TASK-191

- Category: implementation
- Request: Implement `TASK-184` through `TASK-191` for persisted Use Case persistence, lifecycle, readability, and regression coverage.
- Action: Changed backend bulk save to true replace semantics with omitted-row deletion and retained-row rekey support; added retry-to-save recovery for generated drafts; unified persisted diagram CTAs with the shared lifecycle model; redesigned the persisted Use Case route into a read-first artifact page with compact step/branch summaries and localized expand-to-edit panels; and added deterministic Playwright coverage for artifact-specific BRD assertions plus desktop/mobile readability checks.
- Files: `apps/api/app/services/persistence_service.py`, `apps/api/tests/test_persistence_auth_matrix.py`, `src/usecases/PersistedUseCaseWorkspace.tsx`, `src/usecases/PersistedUseCaseWorkspace.test.tsx`, `src/styles.css`, `e2e/artifact-tree.spec.ts`, `README.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`, `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: None; broader suites can now build on the persisted route as the canonical Use Case review surface.
- Status: Done

### 2026-06-08 09:18 - Re-review TASK-184 to TASK-191 and fix camera re-id actor drift

- Category: review
- Request: Re-review `TASK-184` through `TASK-191`, investigate why camera re-id use cases collapse
  to `Ban quản lý` instead of preserving camera/AI actors, and move use-case prompts out of
  hardcoded Python strings into markdown assets.
- Action: Re-reviewed the persisted Use Case implementation and found one real route regression plus
  two generation-layer issues: canonical `FeatureIntent.actors` were not preserved through
  grounding/deterministic fallback, and the synthesis prompt was still hardcoded in the registry.
  Fixed the actor-preservation path across grounding, quality, and deterministic generation; moved
  prompts to versioned markdown assets with a stronger `usecase_synthesis@1.1.0` contract for
  camera/AI/re-id domains; and hardened the persisted deep-link loading state.
- Files: `apps/api/app/ai/prompts/assets/usecase_synthesis/1.1.0/system.md`,
  `apps/api/app/ai/prompts/registry.py`, `apps/api/app/usecases/{actor_signals,grounding,quality,deterministic_builder,generation_service}.py`,
  `apps/api/tests/test_{prompt_registry,usecase_synthesis,usecase_generation_service,usecase_builder}.py`,
  `src/application/ProjectWorkspace.tsx`, `src/application/ProjectWorkspace.test.tsx`,
  `README.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`,
  `docs/progress/{known-issues,changelog}.md`, `docs/reviews/2026-06-08-task-184-191-and-usecase-prompt-review.md`,
  `docs/review-task-list.md`, `docs/activity-log/2026-06.md`
- Follow-up: `TASK-192` to surface generation source/fallback more clearly in the persisted UI.
- Status: Done

### 2026-06-08 09:45 - Implement TASK-192 persisted generation metadata visibility

- Category: implementation
- Request: Implement `TASK-192` so the persisted Use Case flow clearly shows whether the latest
  generation came from AI or deterministic fallback, including provider/model and prompt version.
- Action: Persisted the latest use-case generation metadata on `Feature Intent`, surfaced it on the
  persisted Use Case list and single-item editor routes, merged the metadata into the active feature
  state immediately after generate, and added API/UI regressions for reload-safe visibility.
- Files: `apps/api/app/{models,routes/persistence.py,schemas/persistence.py,services/persistence_generation.py,services/persistence_serializers.py}`,
  `apps/api/alembic/versions/20260608_01_feature_usecase_generation_metadata.py`,
  `apps/api/tests/test_persistence_auth_matrix.py`,
  `src/{application/ProjectWorkspace.tsx,persistence/types.ts,usecases/PersistedUseCaseWorkspace.tsx,usecases/PersistedUseCaseWorkspace.test.tsx,styles.css}`,
  `README.md`, `docs/use-cases/UC-07-sinh-usecase-tu-spec.md`,
  `docs/review-task-list.md`, `docs/progress/changelog.md`, `docs/activity-log/2026-06.md`
- Follow-up: None.
- Status: Done

### 2026-06-08 10:08 - Review TASK-192 implementation

- Category: review
- Request: Review the implementation of `TASK-192`.
- Action: Reviewed the new persisted generation-metadata flow across backend persistence,
  frontend workspace orchestration, UI presentation, and regression coverage. Confirmed that the
  feature now exposes AI-vs-rule provenance after reload, but documented two follow-up defects:
  committed provenance is written before the generated portfolio is saved, and provenance is not
  invalidated when Feature Intent changes after generation.
- Files: `docs/reviews/2026-06-08-task-192-implementation-review.md`,
  `docs/review-task-list.md`, `docs/progress/known-issues.md`, `docs/activity-log/2026-06.md`,
  `apps/api/app/services/persistence_generation.py`, `apps/api/app/services/persistence_service.py`,
  `src/application/ProjectWorkspace.tsx`, `src/usecases/PersistedUseCaseWorkspace.tsx`,
  `apps/api/tests/test_persistence_auth_matrix.py`, `src/usecases/PersistedUseCaseWorkspace.test.tsx`
- Follow-up: `TASK-193` and `TASK-194`.
- Status: Done

### 2026-06-08 12:14 - Implement TASK-193 committed use-case provenance semantics

- Category: implementation
- Request: Implement `TASK-193` so persisted use-case provenance only commits with the saved
  portfolio revision instead of being written during generate.
- Action: Removed provenance persistence from the generate endpoint, added
  `committed_generation_metadata` to the bulk save contract, committed metadata only on successful
  use-case portfolio save, kept session-local metadata visible as a labeled pending state, and
  added API/UI regressions for the generate-success/save-fail path.
- Files: `apps/api/app/{routes/persistence.py,schemas/persistence.py,services/persistence_generation.py,services/persistence_service.py}`,
  `apps/api/tests/test_persistence_auth_matrix.py`,
  `src/{application/ProjectWorkspace.tsx,persistence/WorkspaceContext.tsx,persistence/api.ts,usecases/PersistedUseCaseWorkspace.tsx,usecases/PersistedUseCaseWorkspace.test.tsx,styles.css}`,
  `docs/review-task-list.md`, `docs/progress/{changelog,known-issues}.md`,
  `docs/activity-log/2026-06.md`
- Follow-up: `TASK-194`.
- Status: Done
